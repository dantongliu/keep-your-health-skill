# keep-your-health Skill

## 描述
家庭健康档案智能管理工具。支持体检报告、检查报告的解析、归档与智能分析，并**严格输出高保真的独立 HTML 档案页面**。

## 触发词
- 我要创建健康档案
- 帮我增加报告内容
- 上传截图
- 上传照片
- keep-your-health
- 增加健康信息
- 体检报告录入
- 建档
- 生成健康档案

## 交互与工作流程 (Workflow)
**唤醒方式**：用户通过 `@keep-your-health 我要创建健康档案` 或其他触发词唤醒。

**唤醒后严格执行以下引导步骤：**
- **Step 1. 询问意图**：唤醒后，Skill 必须首先询问用户：“是需要**新建用户档案**，还是**更新已有档案**？”
- **Step 2. 新建档案流程**：若用户选择新建，Skill 需通过提问引导采集个人基础信息（包含年龄、性别、核心病史等）。同时，明确支持并引导用户拍照上传既往病历、检查单，Skill 会自动提取关键信息完善初始档案，为后续评估提供完整依据。
- **Step 3. 更新档案流程**：若用户选择更新已有档案，引导用户上传最新的检查报告照片或 PDF。Skill 收到后自动分类存储、提取新指标，并**完整保留历史记录**（将历史与最新数据合并呈现于趋势图和历史对比表格中），形成连续、可追溯的健康时间线数据。

**底层处理与输出规范：**
- **全量解析**：不管新建还是更新，绝对不能只抓“异常项”！必须全量提取所有正常与异常指标，并生成通俗的医学解读和复查建议。
- **高保真模板注入**：将最新数据写入 `members/{name}/{name}-health-report.html`。必须严格以本 Skill 内部附带的 `assets/onepage-template.html` 为标准参照模板进行 1:1 结构复刻。

## 开发与产出规范 (CRITICAL)
- **绝对不准降级 UI 与交互**：生成的 HTML 必须包含标准模板（以森森的档案为基准）的所有核心模块：
  - **顶部信息栏 (Info Bar)**：必须包含姓名、年龄/性别、核心病史、整体状态。注意：`hdr-title` 的命名**必须严格统一为 `{name}的健康档案`**（如：森森的健康档案），绝对不能增加“独立”、“报告”等冗余字样，浏览器 `<title>` 也必须保持一致。
  - **分类导航 Tab**：包含各系统分类的胶囊按钮，且 JS 切换逻辑必须有效（`switchCat`）。
  - **左右分栏布局**：左侧是带展开箭头 (`▼`) 的指标表格，右侧是直观的横向趋势条/图表区。
  - **动态交互要求 (CRITICAL)**：
    1. **折叠展开 (`toggleRow`)**：指标表格必须支持点击展开/收起（基于 `.clickable`, `.detail-row`, `.show`, `.expanded`），箭头需有 `transform:rotate(180deg)` 动效。
    2. **悬浮提示 (Tooltip)**：右侧趋势图的柱状条 (`.t-bar`) 必须注入对应的 `data-tip` 属性（如 `data-tip="11.74"`），同时严禁遗漏与之配套的 `t-bar::after` 与 `t-bar:hover::after` CSS 悬浮样式。
  - **正常项收纳区 (Ok-chip)**：正常的指标自动收纳进下方的小药丸形状里。
  - **底部复查卡片**：结构化的复查建议模块。
- **输出语调**：专业且温暖，像一个懂医学的朋友，把复杂的医学术语翻译成大白话。